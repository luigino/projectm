subdir('libprojectM')

if enable_jack
   subdir('projectM-jack')
endif

if enable_sdl
   subdir('projectM-sdl')
   subdir('projectM-test')
endif

if enable_qt
   subdir('projectM-qt')
   subdir('projectM-pulseaudio')
endif

if enable_emscripten
   subdir('projectM-emscripten')
endif

